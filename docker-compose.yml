services:
  adk-agent:
    build:
      context: .
    container_name: google-adk-agent
    env_file:
      - .env
    environment:
      ADK_SERVER_MODE: ${ADK_SERVER_MODE:-api}
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    volumes:
      - ./data:/data
      - .:/app
