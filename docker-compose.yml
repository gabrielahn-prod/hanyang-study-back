x-adk-common: &adk-common
  build:
    context: .
  env_file:
    - .env
  environment:
    OLLAMA_API_BASE: ${OLLAMA_API_BASE:-http://host.docker.internal:11434}
    EVERYTIME_MCP_URL: ${EVERYTIME_MCP_URL:-http://everytime-mcp:8001/mcp}
    EVERYTIME_MCP_TIMEOUT: ${EVERYTIME_MCP_TIMEOUT:-10}
  ports:
    - "${PORT:-8000}:8000"
  volumes:
    - ./data:/data
    - .:/app
  networks:
    - default
    - everytime-net
  extra_hosts:
    - "host.docker.internal:host-gateway"

services:
  adk-agent:
    <<: *adk-common
    container_name: google-adk-agent
    command: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]

  adk-web:
    <<: *adk-common
    container_name: google-adk-web
    profiles: ["web"]
    command:
      ["python", "-m", "google.adk.cli", "web", "--host", "0.0.0.0", "--port", "8000", "agents"]

networks:
  everytime-net:
    external: true
    name: everytime-net
